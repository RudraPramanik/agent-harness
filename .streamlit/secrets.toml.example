# Copy to Streamlit Community Cloud → App settings → Secrets (TOML).
# Do not commit real values. Local dev: copy to .streamlit/secrets.toml (gitignored).

AGENT_URL = "https://your-service.onrender.com"
AUTH_SECRET = "same-long-random-string-as-render"
PYTHONPATH = "src"
